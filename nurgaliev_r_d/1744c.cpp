#include <iostream>
#include <vector>
#include <string>

int main() {
    int t;
    std::cin >> t;
    for (int b = 0; b < t; ++b) {
        int n;
        char colour;
        std::string word;
        std::cin >> n >> colour >> word;
    }
}
